import { AccordionItem } from "@dataesr/react-dsfr";
import styled from "styled-components";

const AccordionItemStyled = styled(AccordionItem)`
    box-sizing: border-box;
`;

export default AccordionItemStyled;
